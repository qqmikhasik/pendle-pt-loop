# Core framework — must use per Project 2 spec
fractal-defi==1.3.1

# Lower bounds aligned with fractal-defi 1.3.1's transitive constraints.
numpy>=2.2.6
pandas>=2.3.3
scipy>=1.11
matplotlib>=3.8
jupyter>=1.0

# Data sources
requests>=2.33.1
web3>=6.20
python-dotenv>=1.0

# Risk math (first-passage probability)
# scipy.special provides erfc; no extra dep needed.

# Testing
pytest>=9.0.3
pytest-cov>=5.0
